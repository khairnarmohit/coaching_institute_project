<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Employee Login | Government Competitive Academy</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
:root{
  --gov-dark:#0b1c2d;
  --gov-accent:#f59e0b;
  --gov-bg:#f8fafc;
  --gov-white:#ffffff;
  --gov-text:#1e293b;
  --gov-muted:#475569;
  --gov-border:#e5e7eb;
  --gov-green:#16a34a;
}

/* ===== PAGE ===== */
body{
  min-height:100vh;
  background:
    linear-gradient(135deg, rgba(11,28,45,.95), rgba(11,28,45,.85)),
    url("https://images.unsplash.com/photo-1523580846011-d3a5bc25702b?auto=format&fit=crop&w=2000&q=80")
    center/cover no-repeat;
  font-family:system-ui,-apple-system,BlinkMacSystemFont;
  color:var(--gov-white);
}

/* ===== HEADER ===== */
.top-bar{
  height:64px;
  display:flex;
  align-items:center;
  padding:0 40px;
  background:rgba(11,28,45,.6);
  backdrop-filter:blur(6px);
  border-bottom:1px solid rgba(255,255,255,.1);
}
.logo{
  display:flex;
  align-items:center;
  gap:12px;
  font-weight:800;
  letter-spacing:.3px;
}
.logo img{
  height:38px;
}

/* ===== MAIN ===== */
.login-wrapper{
  min-height:calc(100vh - 64px);
  display:flex;
  align-items:center;
  padding:50px;
}

/* LEFT CONTENT */
.login-info{
  flex:1;
  padding-right:70px;
}
.login-info h1{
  font-weight:900;
  margin-bottom:18px;
}
.login-info p{
  opacity:.9;
  max-width:520px;
  line-height:1.6;
}
.login-info .contact{
  margin-top:25px;
  font-size:14px;
  color:#e5e7eb;
}
.login-info .contact i{
  color:var(--gov-accent);
  margin-right:6px;
}

/* RIGHT CARD */
.login-card{
  width:390px;
  background:var(--gov-white);
  color:var(--gov-text);
  padding:32px;
  border-radius:18px;
  box-shadow:0 30px 70px rgba(0,0,0,.35);
}

.login-card h5{
  font-weight:800;
  margin-bottom:22px;
  color:var(--gov-dark);
  text-align:center;
}

.form-label{
  font-weight:600;
  font-size:14px;
}
.form-control{
  border-radius:12px;
  padding:12px;
  border:1px solid var(--gov-border);
}
.form-control:focus{
  box-shadow:none;
  border-color:var(--gov-accent);
}

/* LOGIN BUTTON */
.login-btn{
  background:var(--gov-accent);
  border:none;
  width:100%;
  padding:13px;
  font-weight:800;
  border-radius:30px;
  color:#000;
  margin-top:15px;
  transition:.3s;
}
.login-btn:hover{
  background:var(--gov-dark);
  color:var(--gov-accent);
}

/* FOOTER */
.footer{
  position:absolute;
  bottom:14px;
  width:100%;
  text-align:center;
  font-size:13px;
  color:#e5e7eb;
}

/* RESPONSIVE */
@media(max-width:768px){
  .login-wrapper{
    flex-direction:column;
    padding:25px;
  }
  .login-info{
    padding-right:0;
    margin-bottom:35px;
    text-align:center;
  }
}
</style>
</head>

<body>

<!-- HEADER -->
<div class="top-bar">
  <div class="logo">
    <img src="https://upload.wikimedia.org/wikipedia/commons/8/8e/Indian_Emblem.svg" alt="">
    Government Competitive Academy
  </div>
</div>

<!-- MAIN -->
<div class="login-wrapper">

  <!-- LEFT -->
  <div class="login-info">
    <h1>Employee Login Portal</h1>
    <p>
      Official internal system of <b>Government Competitive Academy</b>.
      This portal is strictly for authorized faculty, administrative staff,
      and management to handle admissions, student records, examinations,
      results, and institutional operations.
    </p>

    <div class="contact">
      <div><i class="fas fa-phone"></i> Helpline: +91 90000 00000</div>
      <div><i class="fas fa-envelope"></i> support@govtcompetitiveacademy.in</div>
      <div><i class="fas fa-globe"></i> www.govtcompetitiveacademy.in</div>
    </div>
  </div>

  <!-- RIGHT -->
  <div class="login-card">
    <h5><i class="fas fa-lock"></i> Secure Employee Login</h5>

    <form method="post" action="/employee/login">
      <div class="mb-3">
        <label class="form-label">Username / Email</label>
        <input type="email" class="form-control" placeholder="Enter registered email" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Password</label>
        <input type="password" class="form-control" placeholder="Enter password" required>
      </div>

      <button class="login-btn">
        Login Securely
      </button>
    </form>
  </div>

</div>

<div class="footer">
  © 2026 Government Competitive Academy | All Rights Reserved
</div>

</body>
</html>
