<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results | GCA-Institute</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --gov-dark: #0b1c2d;
            --gov-accent: #f59e0b;
        }

        body {
            background-color: #f8fafc;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        }


        /* Main hero animation */
        .results-hero {
            background: linear-gradient(rgba(11, 28, 45, 0.9), rgba(11, 28, 45, 0.8)),
                url("https://images.unsplash.com/photo-1523240795612-9a054b0db644?auto=format&fit=crop&w=2000&q=80") center/cover no-repeat;
            padding: 150px 0 110px;
            text-align: center;
            position: relative;
            overflow: hidden;
            color: #fff;

            opacity: 0;
            animation: heroZoom 1.8s ease-out forwards;
        }

        /* Soft moving glow */
        .results-hero::after {
            content: "";
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at top,
                    rgba(245, 158, 11, 0.18),
                    transparent 65%);
            animation: glowFloat 7s ease-in-out infinite;
            pointer-events: none;
        }

        /* Hero zoom animation */
        @keyframes heroZoom {
            from {
                opacity: 0;
                transform: scale(1.08);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        /* Glow floating */
        @keyframes glowFloat {
            0% {
                opacity: 0.4;
            }

            50% {
                opacity: 0.7;
            }

            100% {
                opacity: 0.4;
            }
        }

        /* Content layer */
        .results-hero .container {
            position: relative;
            z-index: 2;
        }

        /* Title animation */
        .results-hero h1 {
            font-weight: 800;
            opacity: 0;
            animation: titleReveal 1s ease-out forwards;
            animation-delay: 0.4s;
        }

        /* Highlight gradient */
        .results-hero .highlight {
            background: linear-gradient(135deg, #f59e0b, #fde68a);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Subtitle animation */
        /* Subtitle – Glassmorphism */
        .results-hero p {
            opacity: 0;
            animation: subtitleReveal 1s ease-out forwards;
            animation-delay: 0.9s;

            display: inline-block;
            margin-top: 20px;
            padding: 14px 30px;

            color: #f1f5f9;
            font-size: 1.2rem;
            font-weight: 500;

            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);

            border-radius: 40px;
            border: 1px solid rgba(255, 255, 255, 0.25);

            box-shadow:
                0 10px 30px rgba(0, 0, 0, 0.25),
                inset 0 1px 1px rgba(255, 255, 255, 0.2);
        }


        /* Text animations */
        @keyframes titleReveal {
            from {
                opacity: 0;
                transform: translateY(30px);
                filter: blur(6px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
                filter: blur(0);
            }
        }

        @keyframes subtitleReveal {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .results-hero h1 {
                font-size: 2.2rem;
            }
        }



        /* Topper Card */
        .topper-card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            text-align: center;
            padding: 20px 15px;
            /* Reduced vertical padding */
            border-top: 5px solid var(--gov-accent);
            height: 100%;
        }

        .topper-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.12);
        }

        .topper-img-wrapper {
            width: 130px;
            /* Increased image size */
            height: 130px;
            margin: 0 auto 15px;
            border-radius: 50%;
            border: 4px solid #fff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        .topper-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }

        .rank-badge {
            background: var(--gov-accent);
            color: var(--gov-dark);
            font-weight: bold;
            padding: 5px 15px;
            border-radius: 20px;
            display: inline-block;
            margin-bottom: 10px;
            /* Reduced margin */
            font-size: 0.9rem;
        }

        .student-name {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--gov-dark);
            margin-bottom: 5px;
        }

        .course-name {
            color: #64748b;
            font-weight: 500;
            margin-bottom: 10px;
        }

        .score {
            font-size: 1.1rem;
            font-weight: 600;
            color: #bc8a10;
            /* Goldish */
        }

        .laurels-icon {
            font-size: 1.8rem;
            /* Slightly reduced icon size */
            color: #e2e8f0;
            margin-bottom: 5px;
        }
    </style>
</head>

<body>

    <%- include('navbar') %>

        <!-- Hero Header -->
        <header class="results-hero">
            <div class="container">
                <h1 class="display-4">Toppers In Our <span class="highlight">Hall of Fame</span></h1>
                <p class="lead text-light opacity-75">Celebrating the outstanding achievements of our students.</p>
            </div>
        </header>

        <!-- Results Grid -->
        <section class="py-5">
            <div class="container">

                <div class="row g-4 justify-content-center">

                    <% if (toppers && toppers.length> 0) { %>
                        <!-- Limit to first 5 toppers -->
                        <% toppers.slice(0, 5).forEach(student=> { %>
                            <div class="col-lg-4 col-md-6"> <!-- Changed to col-lg-4 for 3 per row -->
                                <div class="topper-card">
                                    <div class="laurels-icon">
                                        <i class="fas fa-trophy text-warning"></i>
                                    </div>

                                    <div class="topper-img-wrapper">
                                        <!-- Check if image exists, otherwise show placeholder -->
                                        <% if(student.image) { %>
                                            <img src="/images/<%= student.image %>" alt="<%= student.name %>"
                                                class="topper-img">
                                            <% } else { %>
                                                <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png"
                                                    alt="Student" class="topper-img">
                                                <% } %>
                                    </div>

                                    <div class="rank-badge">
                                        <i class="fas fa-star me-1"></i>
                                        <%= student.rank %>
                                    </div>

                                    <h3 class="student-name">
                                        <%= student.name %>
                                    </h3>
                                    <div class="course-name">
                                        <%= student.course %>
                                    </div>

                                    <% if(student.percentage) { %>
                                        <div class="score">Score: <%= student.percentage %>
                                        </div>
                                        <% } %>

                                            <small class="text-muted d-block mt-2">Batch: <%= student.batch_year %>
                                            </small>
                                </div>
                            </div>
                            <% }); %>
                                <% } else { %>
                                    <div class="col-12 text-center py-5">
                                        <h4 class="text-muted">Results will be announced soon!</h4>
                                        <img src="https://cdn-icons-png.flaticon.com/512/7486/7486744.png" width="100"
                                            class="mt-3 opacity-50">
                                    </div>
                                    <% } %>

                </div>
            </div>
        </section>

        <!-- Stats / Call to Action -->
        <section class="py-5 bg-white">
            <div class="container text-center">
                <div class="row justify-content-center">
                    <div class="col-lg-8">
                        <h2 class="fw-bold mb-4">Be the Next Topper!</h2>
                        <p class="mb-4 text-muted">Join our structured courses and get the guidance you need to succeed.
                        </p>
                        <a href="/admission" class="btn btn-warning btn-lg text-dark fw-bold px-5">Join Now</a>
                    </div>
                </div>
            </div>
        </section>

        <%- include('footer') %>

            <!-- Bootstrap JS -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>