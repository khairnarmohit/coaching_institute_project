<%-include("navbar.ejs")%>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Courses | Competitive Academy</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>


    /* Animations */
    @keyframes fadeUp {
      0% { opacity: 0; transform: translateY(40px); }
      100% { opacity: 1; transform: translateY(0);}
    }

    @keyframes fadeRight {
      0% { opacity: 0; transform: translateX(-40px); }
      100% { opacity: 1; transform: translateX(0);}
    }

    @keyframes fadeLeft {
      0% { opacity: 0; transform: translateX(40px); }
      100% { opacity: 1; transform: translateX(0);}
    }

    @keyframes zoomIn {
      0% { opacity: 0; transform: scale(0.8); }
      100% { opacity: 1; transform: scale(1);}
    }

    @keyframes zoomOut {
      0% { opacity: 0; transform: scale(1.2); }
      100% { opacity: 1; transform: scale(1);}
    }

    @keyframes slideUp {
      0% { opacity: 0; transform: translateY(60px); }
      100% { opacity: 1; transform: translateY(0);}
    }

    /* Attach animations per card */
    .card-1 { animation: fadeUp .8s forwards ease; }
    .card-2 { animation: fadeRight .9s forwards ease; }
    .card-3 { animation: fadeLeft .9s forwards ease; }
    .card-4 { animation: zoomIn .8s forwards ease; }
    .card-5 { animation: slideUp .9s forwards ease; }
    .card-6 { animation: zoomOut .9s forwards ease; }

    .course-title {
      font-size: 20px;
      font-weight: 700;
      color: #1b1b1b;
    }

    .course-desc {
      font-size: 14px;
      color: #6b6b6b;
      margin-bottom: 15px;
    }

    .btn-view {
      font-size: 14px;
      font-weight: 600;
      border-radius: 8px;
      padding: 6px 16px;
    }

 
:root {
  --gov-dark: #0b1c2d;
  --gov-accent: #f59e0b;
  --gov-bg: #f8fafc;
  --gov-white: #ffffff;
  --gov-text: #1e293b;
  --gov-muted: #475569;
  --gov-border: #e5e7eb;
  --gov-green: #16a34a;
}

body {
  background: var(--gov-bg);
  font-family: "Poppins", sans-serif;
  color: var(--gov-text);
}

.course-card {
  background: var(--gov-white);
  border-radius: 14px;
  padding: 22px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.08);
  border-left: 5px solid var(--gov-green); /* One side border */
  min-height: 160px;
  opacity: 0;
}


.course-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--gov-dark);
}

.course-desc {
  font-size: 14px;
  color: var(--gov-muted);
  margin-bottom: 15px;
}

.btn-view {
  background: var(--gov-accent);
  border-color: var(--gov-accent);
  font-size: 14px;
  font-weight: 600;
  border-radius: 8px;
  padding: 6px 16px;
}

.btn-view:hover {
  opacity: .85;
}
</style>

  </style>
</head>

<body>

<div class="container py-5">

  <h2 class="text-center fw-bold mb-3">Our Courses</h2>
  <p class="text-center text-muted mb-4">Select a course to explore</p>

  <div class="row g-4">

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-1">
        <div class="course-title">Talathi Exam</div>
        <p class="course-desc">Talathi recruitment preparation.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-2">
        <div class="course-title">Police Bharti</div>
        <p class="course-desc">Written + Physical + Interview preparation.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-3">
        <div class="course-title">Banking Exams</div>
        <p class="course-desc">IBPS / SBI / RRB coaching.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-4">
        <div class="course-title">MPSC</div>
        <p class="course-desc">Pre + Mains + Interview guidance.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-5">
        <div class="course-title">Railway Exams</div>
        <p class="course-desc">RRB recruitment preparation.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

    <div class="col-md-4 col-sm-6">
      <div class="course-card card-6">
        <div class="course-title">UPSC</div>
        <p class="course-desc">IAS / IPS full coaching.</p>
        <a href="#" class="btn btn-primary btn-view">View Details</a>
      </div>
    </div>

  </div>
</div>

</body>
</html>






<%-include("footer.ejs")%>