<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Course Details - GCA</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --gov-dark: #0b1c2d;
      --gov-accent: #f59e0b;
      --gov-bg: #f8fafc;
      --gov-white: #ffffff;
      --gov-text: #1e293b;
      --gov-muted: #475569;
      --gov-border: #e5e7eb;
      --gov-green: #16a34a;
    }

    body {
      background: var(--gov-bg);
      font-family: system-ui, -apple-system, sans-serif;
      color: var(--gov-text);
      overflow: hidden;
    }

    /* TOP HEADER */
    .detail-header {
      background: linear-gradient(rgba(11, 28, 45, 0.9), rgba(11, 28, 45, 0.85)),
        url("<%= course.image %>") center/cover no-repeat;
      padding: 100px 0;
      color: var(--gov-white);
      text-align: center;
    }

    .detail-header h1 {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 15px;
    }

    /* CONTENT LAYOUT */
    .detail-container {
      margin-top: -50px;
      position: relative;
      z-index: 10;
      margin-bottom: 80px;
    }

    .main-info-card {
      background: var(--gov-white);
      border-radius: 20px;
      box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
      padding: 50px;
      margin-bottom: 40px;
    }

    .section-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--gov-dark);
      margin-bottom: 25px;
      border-left: 6px solid var(--gov-accent);
      padding-left: 20px;
    }

    .course-full-desc {
      font-size: 1.15rem;
      line-height: 1.8;
      color: var(--gov-muted);
      margin-bottom: 40px;
    }

    /* QUICK STATS */
    .quick-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 50px;
    }

    .stat-box {
      background: var(--gov-bg);
      padding: 25px;
      border-radius: 15px;
      text-align: center;
      border: 1px solid var(--gov-border);
    }

    .stat-box i {
      font-size: 2rem;
      color: var(--gov-accent);
      margin-bottom: 15px;
    }

    .stat-box h5 {
      font-weight: 700;
      color: var(--gov-dark);
      margin-bottom: 5px;
    }

    .stat-box p {
      margin: 0;
      color: var(--gov-muted);
      font-weight: 600;
    }

    /* FEATURES GRID */
    .features-list {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 15px;
    }

    .feature-item {
      display: flex;
      align-items: center;
      gap: 12px;
      background: white;
      padding: 15px 20px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.03);
      border-left: 4px solid var(--gov-green);
    }

    .feature-item i {
      color: var(--gov-green);
    }

    /* SIDEBAR CARD */
    .apply-card {
      background: var(--gov-dark);
      color: var(--gov-white);
      padding: 40px;
      border-radius: 20px;
      position: sticky;
      top: 100px;
      text-align: center;
    }

    .apply-card h3 {
      font-weight: 700;
      margin-bottom: 10px;
    }

    .price-tag {
      font-size: 2rem;
      font-weight: 800;
      color: var(--gov-accent);
      margin: 20px 0;
    }

    .btn-apply {
      background: var(--gov-accent);
      color: var(--gov-dark);
      padding: 15px 30px;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1.1rem;
      text-decoration: none;
      display: block;
      transition: all 0.3s ease;
    }

    .btn-apply:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 20px rgba(245, 158, 11, 0.3);
    }

    @media (max-width: 992px) {
      .detail-container {
        margin-top: 20px;
      }
    }
  </style>
</head>

<body>
  <%-include("navbar.ejs")%>

    <div class="detail-header">
      <div class="container">
        <h1>
          <%= course.title %>
        </h1>
        <p class="lead">
          <%= course.shortDesc %>
        </p>
      </div>
    </div>

    <div class="container detail-container">
      <div class="row g-5">
        <div class="col-lg-8">
          <div class="main-info-card">
            <h2 class="section-title">Course Overview</h2>
            <p class="course-full-desc">
              <%= course.fullDesc %>
            </p>

            <div class="quick-stats">
              <div class="stat-box">
                <i class="fas fa-hourglass-half"></i>
                <h5>Duration</h5>
                <p>
                  <%= course.duration %>
                </p>
              </div>
              <div class="stat-box">
                <i class="fas fa-user-graduate"></i>
                <h5>Eligibility</h5>
                <p>
                  <%= course.eligibility %>
                </p>
              </div>
            </div>

            <h2 class="section-title">What You'll Get</h2>
            <div class="features-list">
              <% course.features.forEach(feature=> { %>
                <div class="feature-item">
                  <i class="fas fa-check-circle"></i>
                  <span>
                    <%= feature %>
                  </span>
                </div>
                <% }); %>
            </div>

            <div class="mt-5 p-4 rounded-4 bg-light border">
              <h5 class="fw-bold mb-3">Admission Notice</h5>
              <p class="text-muted mb-0">
                <%= course.admission_notice%>
              </p>
            </div>

          </div>
        </div>

        <div class="col-lg-4">
          <div class="apply-card">
            <h3>Start Learning Today</h3>
            <p class="opacity-75">Invest in your future with the best mentorship in the industry.</p>
            <div class="price-tag">
              <% if (course.fees_structure) { %>
                ₹ <%= course.fees_structure.replace('-', ' - ₹' ) %>
                  <% } else { %>
                    <span>Fees will be announced soon</span>
                    <% } %>
            </div>
            <a href="/admission" class="btn-apply">Enroll Now</a>
            <p class="mt-4 small opacity-50">*EMI options available for comprehensive courses.</p>

            <div class="mt-5 text-start border-top pt-4">
              <h6 class="fw-bold mb-3">Got Questions?</h6>
              <div class="d-flex align-items-center gap-3 mb-3">
                <i class="fas fa-phone-alt text-accent"></i>
                <div>
                  <div class="small opacity-50">Call Support</div>
                  <div class="fw-bold">+91 98765 43210</div>
                </div>
              </div>
              <div class="d-flex align-items-center gap-3">
                <i class="fas fa-envelope text-accent"></i>
                <div>
                  <div class="small opacity-50">Email Us</div>
                  <div class="fw-bold">info@eduinstitute.com</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%-include("footer.ejs")%>
      <!-- Bootstrap JS -->
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>