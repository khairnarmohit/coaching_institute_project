<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admission Form - GCA</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --gov-dark: #0b1c2d;
            --gov-accent: #f59e0b;
            --gov-bg: #f8fafc;
            --gov-white: #ffffff;
            --gov-text: #1e293b;
            --gov-muted: #475569;
            --gov-border: #e5e7eb;
        }


        /* PREMIUM HERO SECTION */
        .admission-hero {
            background: linear-gradient(rgba(11, 28, 45, 0.9), rgba(11, 28, 45, 0.8)),
                url("https://images.unsplash.com/photo-152305085306e-880005f847b3?ixlib=rb-4.0.3&auto=format&fit=crop&w=2000&q=80") center/cover no-repeat;
            padding: 100px 0;
            text-align: center;
            color: var(--gov-white);
        }

        .admission-hero h1 {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 15px;
        }

        /* FORM DESIGN */
        .admission-form-container {
            max-width: 900px;
            margin: -50px auto 100px;
            background: var(--gov-white);
            padding: 50px;
            border-radius: 30px;
            box-shadow: 0 20px 40px rgba(11, 28, 45, 0.1);
            position: relative;
            z-index: 10;
            border: 1px solid var(--gov-border);
        }

        .form-section-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--gov-dark);
            margin-bottom: 25px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--gov-accent);
            display: inline-block;
        }

        .form-label {
            font-weight: 600;
            color: var(--gov-dark);
            margin-bottom: 8px;
            font-size: 0.9rem;
        }

        .form-control {
            padding: 12px 18px;
            border-radius: 12px;
            border: 1px solid var(--gov-border);
            background: var(--gov-bg);
            transition: all 0.3s ease;
            font-size: 0.95rem;
        }

        .form-control:focus {
            background: var(--gov-white);
            border-color: var(--gov-accent);
            box-shadow: 0 0 0 4px rgba(245, 158, 11, 0.1);
        }

        .btn-submit {
            background: #ffcc00;
            /* Vibrant Yellow */
            color: var(--gov-dark);
            padding: 15px 40px;
            border-radius: 50px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: none;
            transition: all 0.4s ease;
            width: 100%;
            margin-top: 30px;
            box-shadow: 0 4px 15px rgba(255, 204, 0, 0.3);
        }

        .btn-submit:hover {
            background: #ffdb1f;
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 204, 0, 0.5);
        }

        .section-card {
            background: var(--gov-bg);
            padding: 30px;
            border-radius: 20px;
            margin-bottom: 40px;
            border: 1px solid var(--gov-border);
        }

        /* REQUIRED MARKER */
        .required-star {
            color: #ef4444;
            margin-left: 3px;
        }
    </style>
</head>

<body>
    <%-include("navbar.ejs")%>

        <!-- Add AOS -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css">

        <div class="admission-hero">
            <div class="container">
                <h1 data-aos="fade-up">Enrollment Form</h1>
                <p class="lead" data-aos="fade-up" data-aos-delay="100">Take the first step towards your career in
                    public
                    service. Fill out the form below to apply.</p>
            </div>
        </div>

        <div class="container">
            <form class="admission-form-container" action="/addmission_form" method="post" enctype="multipart/form-data"
                data-aos="fade-up" data-aos-delay="200">

                <!-- Student Identity -->
                <div class="mb-5 text-center">
                    <h3 class="fw-bold" style="color: var(--gov-dark);">Registration Details</h3>
                    <p class="text-muted">Academic Year 2026-27</p>
                </div>

                <!-- Personal Details -->
                <h5 class="form-section-title">Personal Information</h5>
                <div class="section-card">
                    <div class="row g-4">
                        <div class="col-12">
                            <label class="form-label">Full Name<span class="required-star">*</span></label>
                            <input type="text" class="form-control" name="full_name"
                                placeholder="As per official documents" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Gender<span class="required-star">*</span></label>
                            <select class="form-control" name="gender" required>
                                <option value="">-- Choose --</option>
                                <option>Male</option>
                                <option>Female</option>
                                <option>Other</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Date of Birth<span class="required-star">*</span></label>
                            <input type="date" class="form-control" name="date_of_birth" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Mobile Number<span class="required-star">*</span></label>
                            <input type="tel" class="form-control" name="mobile_number" placeholder="+91" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Email Address<span class="required-star">*</span></label>
                            <input type="email" class="form-control" name="email" placeholder="example@mail.com"
                                required>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Permanent Address<span class="required-star">*</span></label>
                            <textarea class="form-control" name="address" rows="3"
                                placeholder="Full residential address" required></textarea>
                        </div>
                    </div>
                </div>

                <!-- Academic Details -->
                <h5 class="form-section-title">Educational Background</h5>
                <div class="section-card">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label class="form-label">Highest Qualification</label>
                            <input type="text" class="form-control" name="qualification" placeholder="e.g. B.A., M.Sc.">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">College/University</label>
                            <input type="text" class="form-control" name="college_name"
                                placeholder="Full name of institute">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Year of Passing</label>
                            <input type="number" class="form-control" name="passing_year" placeholder="YYYY">
                        </div>
                    </div>
                </div>

                <!-- Program Choice -->
                <h5 class="form-section-title">Course Selection</h5>
                <div class="section-card">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label class="form-label">Selected Course<span class="required-star">*</span></label>
                            <select class="form-control" name="course_name" required>
                                <option value="">-- Select Course --</option>
                                <option>UPSC Civil Services</option>
                                <option>MPSC State Services</option>
                                <option>Banking Exams</option>
                                <option>Police Bharti</option>
                                <option>Talathi</option>
                                <option>Railway Exams</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Mode of Study<span class="required-star">*</span></label>
                            <select class="form-control" name="mode_of_class" required>
                                <option>Online (Live Classes)</option>
                                <option>Offline (Campus)</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Parent Details -->
                <h5 class="form-section-title">Guardian Information</h5>
                <div class="section-card">
                    <div class="row g-4">
                        <div class="col-md-7">
                            <label class="form-label">Parent/Guardian Name</label>
                            <input type="text" class="form-control" name="parent_name" placeholder="Full name">
                        </div>
                        <div class="col-md-5">
                            <label class="form-label">Emergency Contact Number</label>
                            <input type="tel" class="form-control" name="parent_contact" placeholder="+91">
                        </div>
                    </div>
                </div>

                <!-- Document Upload -->
                <h5 class="form-section-title">Required Documents</h5>
                <div class="section-card">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label class="form-label">Passport Size Photo</label>
                            <input type="file" class="form-control" name="photo">
                            <small class="text-muted mt-1 d-block">Max 2MB (JPG/PNG)</small>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Identity Proof (Aadhaar/PAN)</label>
                            <input type="file" class="form-control" name="id_proof">
                            <small class="text-muted mt-1 d-block">PDF preferred</small>
                        </div>
                    </div>
                </div>

                <!-- Information Accuracy Declaration -->
                <div class="form-check mt-4">
                    <input class="form-check-input" type="checkbox" id="declareCheck" required>
                    <label class="form-check-label small text-muted" for="declareCheck">
                        I hereby declare that the information provided above is true and accurate to the best of my
                        knowledge.
                    </label>
                </div>

                <button type="submit" class="btn btn-submit">
                    Confirm & Submit Admission
                </button>
            </form>
        </div>

        <!-- Scripts -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
        <script>
            AOS.init({
                duration: 800,
                once: true
            });
        </script>


        <%-include("footer.ejs")%>
            <!-- Bootstrap JS -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>