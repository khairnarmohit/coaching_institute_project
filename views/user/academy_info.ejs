<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Academy Info - GCA</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --gov-dark: #0b1c2d;
      --gov-accent: #f59e0b;
      --gov-bg: #f8fafc;
      --gov-white: #ffffff;
      --gov-text: #1e293b;
      --gov-muted: #475569;
      --gov-border: #e5e7eb;
    }


    /* HEADER */
    .about-header {
      background: linear-gradient(rgba(11, 28, 45, 0.9), rgba(11, 28, 45, 0.75)),
        url("https://images.unsplash.com/photo-1517673132405-a56a62b18caf?ixlib=rb-4.0.3&auto=format&fit=crop&w=2000&q=80") center/cover no-repeat fixed;
      padding: 150px 0;
      color: var(--gov-white);
      text-align: center;
      position: relative;
    }

    .about-header h1 {
      font-size: 4rem;
      font-weight: 800;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
      margin-bottom: 20px;
    }

    .about-header p {
      font-size: 1.25rem;
      max-width: 800px;
      margin: 0 auto;
      opacity: 0.9;
    }

    /* SECTION */
    .about-section {
      padding: 100px 0;
      background: var(--gov-white);
    }

    /* IMAGE */
    .about-img-container {
      position: relative;
    }

    .about-img {
      width: 100%;
      height: 500px;
      object-fit: cover;
      border-radius: 20px;
      box-shadow: 0px 20px 50px rgba(11, 28, 45, 0.2);
      transition: transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .about-img:hover {
      transform: scale(1.02);
    }

    .vm-section {
      background-color: #f8fafc;
    }

    .vm-card {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      padding: 30px;
      border-radius: 12px;
      transition: all 0.3s ease;
    }

    .vm-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
    }

    .vm-icon {
      width: 70px;
      height: 70px;
      background-color: #0b1c2d;
      color: #ffffff;
      font-size: 28px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto;
    }

    .vm-card h4 {
      font-weight: 600;
      color: #0b1c2d;
    }

    .vm-card p {
      color: #475569;
      font-size: 15px;
      line-height: 1.7;
      margin-top: 10px;
    }


    /* CORE VALUES */
    .values-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 30px;
      margin-top: 60px;
    }

    .value-card {
      background: var(--gov-white);
      padding: 35px;
      border-radius: 16px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
      border: 1px solid var(--gov-border);
      transition: all 0.3s ease;
      text-align: center;
    }

    .value-card:hover {
      transform: translateY(-10px);
      border-color: var(--gov-accent);
      box-shadow: 0 15px 40px rgba(11, 28, 45, 0.1);
    }

    .value-card i {
      font-size: 2.5rem;
      color: var(--gov-accent);
      margin-bottom: 20px;
    }

    .value-card h4 {
      font-weight: 700;
      color: var(--gov-dark);
      margin-bottom: 15px;
    }

    /* EXAM FOCUS */
    .exam-focus {
      padding: 100px 0;
      background: var(--gov-dark);
      color: var(--gov-white);
    }

    .exam-badge {
      display: inline-block;
      padding: 10px 20px;
      background: rgba(245, 158, 11, 0.2);
      border: 1px solid var(--gov-accent);
      border-radius: 50px;
      color: var(--gov-accent);
      margin: 5px;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .exam-badge:hover {
      background: var(--gov-accent);
      color: var(--gov-dark);
      transform: scale(1.1);
      box-shadow: 0 5px 15px rgba(245, 158, 11, 0.4);
      cursor: pointer;
    }

    /* TEACHING METHODOLOGY */
    .tm-section {
      background: var(--gov-bg);
      padding: 100px 0;
    }

    .tm-heading {
      font-size: 38px;
      font-weight: 800;
      color: var(--gov-dark);
      margin-bottom: 30px;
      text-align: center;
    }

    .tm-heading::after {
      content: '';
      display: block;
      width: 80px;
      height: 5px;
      background: var(--gov-accent);
      margin: 15px auto;
      border-radius: 10px;
    }

    .tm-list {
      list-style: none;
      padding: 0;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 20px;
    }

    .tm-list li {
      background: var(--gov-white);
      padding: 25px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.03);
      border-left: 5px solid var(--gov-accent);
      display: flex;
      align-items: center;
      gap: 15px;
      transition: transform 0.3s ease;
    }

    .tm-list li:hover {
      transform: scale(1.03);
    }

    .tm-list li i {
      color: var(--gov-accent);
      font-size: 1.25rem;
    }

    .about-title {
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--gov-dark);
      font-size: 2.5rem;
    }

    .about-lead {
      font-size: 1.1rem;
      font-weight: 400;
      margin-bottom: 1rem;
      color: var(--gov-muted);
    }

    .about-description {
      color: var(--gov-muted);
      line-height: 1.8;
      font-size: 1rem;
    }


    /* Mobile */
    @media (max-width: 768px) {
      .about-header h1 {
        font-size: 2.5rem;
      }

      .about-img {
        height: 350px;
        margin-bottom: 40px;
      }
    }
  </style>
</head>

<body>
  <%-include("navbar.ejs")%>

    <div class="about-header">
      <div class="container">
        <h1>Transforming Aspirants Into Officers</h1>
        <p>Leading the way in Government Exam Excellence with dedicated mentorship and proven strategies. Your journey
          to
          a prestigious career starts here.</p>
      </div>
    </div>

    <section class="about-section">
      <div class="container">
        <div class="row g-5 align-items-center">
          <div class="col-lg-6">
            <div class="about-img-container">

              <% if (academyInfo && academyInfo.length> 0) { %>

                <% academyInfo.forEach(function(item){ %>
                  <% if (item.about_image) { %>
                    <img src="/images/<%= item.about_image %>" class="about-img mb-4" alt="Academy Environment">
                    <% } %>
                      <% }) %>

                        <% } else { %>
                          <img src="https://via.placeholder.com/1000x600?text=No+Image" class="about-img"
                            alt="No Image Available">
                          <% } %>

            </div>

          </div>
          <div class="col-lg-6">
            <% if (academyInfo && academyInfo.length> 0 && academyInfo[0].short_info) { %>
              <%- academyInfo[0].short_info %>
                <% } %>
          </div>

        </div>

        <div class="values-grid">
          <div class="value-card">
            <i class="fas fa-user-tie"></i>
            <h4>Expert Mentorship</h4>
            <p>Learn from experienced faculty members who have themselves cleared various stages of UPSC and MPSC exams.
            </p>
          </div>
          <div class="value-card">
            <i class="fas fa-book-open"></i>
            <h4>Comprehensive Study Material</h4>
            <p>Precisely curated notes and books covering every aspect of the syllabus, from NCERT basics to advanced
              topics.</p>
          </div>
          <div class="value-card">
            <i class="fas fa-chart-line"></i>
            <h4>Strategic Test Series</h4>
            <p>Weekly and monthly tests designed on the latest exam patterns to track progress and improve
              answer-writing
              skills.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="vm-section py-5">
      <div class="container">

        <!-- Section Heading -->
        <div class="text-center mb-5">
          <h2 class="fw-bold">Mission & Vision</h2>
          <p class="text-muted">Our purpose, direction and commitment</p>
        </div>

        <!-- Cards -->
        <div class="row g-4 justify-content-center">

          <!-- Vision -->
          <div class="col-md-5">
            <div class="vm-card h-100 text-center">
              <div class="vm-icon">
                <i class="fas fa-eye"></i>
              </div>
              <h4 class="mt-3">Our Vision</h4>
              <p>
                <% if (academyInfo && academyInfo.length> 0 && academyInfo[0].vision) { %>
                  <%- academyInfo[0].vision %>
                    <% } %>
              </p>
            </div>
          </div>

          <!-- Mission -->
          <div class="col-md-5">
            <div class="vm-card h-100 text-center">
              <div class="vm-icon">
                <i class="fas fa-rocket"></i>
              </div>
              <h4 class="mt-3">Our Mission</h4>
              <p>
                <% if (academyInfo && academyInfo.length> 0 && academyInfo[0].mission) { %>
                  <%- academyInfo[0].mission %>
                    <% } %>
              </p>
            </div>
          </div>

        </div>
      </div>
    </section>


    <section class="exam-focus">
      <div class="container text-center">
        <h2 class="fw-bold mb-5">Our Specializations</h2>
        <div class="row justify-content-center">
          <div class="col-md-10">
            <div class="mb-4">
              <% if (academyInfo && academyInfo.length> 0 && academyInfo[0].specializations) { %>
                <% academyInfo[0].specializations.split(',').forEach(function(item){ %>
                  <span class="exam-badge">
                    <%= item.trim() %>
                  </span>
                  <% }) %>

                    <% } %>


            </div>
            <p class="mt-4 opacity-75">We provide integrated batches for all administrative and clerical grade
              examinations with a focus on core conceptual clarity and speed-solving techniques.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="teaching-methodology" class="tm-section">
      <div class="container">
        <h2 class="tm-heading">Teaching Methodology</h2>
        <div class="row mt-5">
          <div class="col-12">
            <ul class="tm-list">

              <% if (academyInfo && academyInfo.length> 0 && academyInfo[0].methodology) { %>

                <% academyInfo[0].methodology.split(',').forEach(function(item){ %>
                  <li>
                    <i class="fas fa-check-circle"></i>
                    <span>
                      <%= item.trim() %>
                    </span>
                  </li>
                  <% }) %>

                    <% } else { %>
                      <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Teaching methodology will be updated soon</span>
                      </li>
                      <% } %>

            </ul>

          </div>
        </div>
      </div>
    </section>

    <%-include("footer.ejs")%>
      <!-- Bootstrap JS -->
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>